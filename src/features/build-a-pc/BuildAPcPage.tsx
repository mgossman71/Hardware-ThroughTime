import { UnderConstruction } from '../../components/UnderConstruction';

export default function BuildAPcPage() {
  return (
    <UnderConstruction
      kicker="03 — BUILD A PC"
      title="Build a PC From This Era"
      lede="Choose a year and pick from the components that were actually on sale at the time — period-correct CPUs, GPUs, RAM, storage, and sound."
      progressNote="era component pools are being researched and populated next."
    />
  );
}