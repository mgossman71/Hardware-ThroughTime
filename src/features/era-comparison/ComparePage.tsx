import { PageShell } from '../../components/PageShell';

export default function ComparePage() {
  return (
    <PageShell
      kicker="04 — THEN VS NOW"
      title="Compare Eras"
      lede="Put two points in time side by side — clock speeds, RAM, storage, graphics, networking — with honest notes where a direct ratio is scientifically misleading."
    >
      <p className="mono" style={{ color: 'var(--text-2)' }}>
        // under construction — era comparison data is being researched and populated next.
      </p>
    </PageShell>
  );
}